services:
  - type: web
    name: smart-hospital
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DB_BACKEND
        value: mongo
      - key: MONGODB_URI
        sync: false
      - key: SECRET_KEY
        generateValue: true
